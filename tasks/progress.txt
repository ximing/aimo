## Codebase Patterns
- Use @rabjs/react for frontend state management with observable stores
- Service classes extend Service from @rabjs/react and use bindServices() for React integration
- API calls use the request wrapper with typed responses
- SM-2 standard formula: EF' = max(1.3, EF + (0.1 - (5-q)*(0.08 + (5-q)*0.02)))
  - q=5: +0.1, q=4: 0 (unchanged), q=3: -0.14, q=1: -0.54 (often clamped to 1.3)
- Interval must use OLD easeFactor (before update) to avoid double penalty on fuzzy ratings

## 2026-03-08 - US-001
- Replaced hardcoded easeFactor offsets (+0.15/+0.1/-0.08/-0.2) with SM-2 standard formula
- Formula: EF' = max(1.3, EF + (0.1 - (5-q)*(0.08 + (5-q)*0.02)))
- Files changed: apps/server/src/services/spaced-repetition.service.ts, apps/server/src/__tests__/spaced-repetition.service.test.ts
- **Learnings for future iterations:**
  - The PRD verification example for q=1 shows "1.3" but actual formula gives 1.96 (not clamped unless EF is very low)
  - The worktree already had the SM-2 formula partially implemented; tests just needed updating
  - Interval calculation already used old EF correctly in the worktree
---

## 2026-03-08 - US-002
- No code changes needed - the worktree already implemented this correctly
- Interval calculation uses old easeFactor; newEaseFactor applied after interval is computed
- The test added in US-001 already covers this: "uses old easeFactor for interval formula (no double penalty)"
- **Learnings for future iterations:**
  - Always check if the worktree already has partial implementations before writing new code
---

## 2026-03-08 - US-003
- Added MAX_INTERVAL_DAYS = 365 constant to spaced-repetition.service.ts
- Applied Math.min(interval, MAX_INTERVAL_DAYS) before returning from calculateNextReview
- Updated tests to verify the cap is applied correctly
- Files changed: apps/server/src/services/spaced-repetition.service.ts, apps/server/src/__tests__/spaced-repetition.service.test.ts
- **Learnings for future iterations:**
  - Simple constant addition with min() cap - straightforward implementation
---

## 2026-03-08 - US-004 & US-005
- Added lapseCount field to schema (int, default 0, NOT NULL)
- Generated migration file: drizzle/0010_mean_barracuda.sql
- Implemented lapseCount logic: increment on quality=1 (forgot), keep unchanged otherwise
- Updated getDueCards API to return lapseCount field
- Updated reviewCard API to persist lapseCount to database
- Added comprehensive tests for lapseCount tracking
- Files changed: apps/server/src/db/schema/spaced-repetition-cards.ts, apps/server/src/services/spaced-repetition.service.ts, apps/server/src/controllers/v1/spaced-repetition.controller.ts, apps/server/src/__tests__/spaced-repetition.service.test.ts
- **Learnings for future iterations:**
  - Schema changes require: 1) update schema file, 2) pnpm build, 3) pnpm migrate:generate
  - Test files need all card objects updated with new fields to pass typecheck
  - Used replace_all to efficiently update repeated test patterns
---

## 2026-03-08 - US-006
- Added 'recent_days' and 'date_range' to filterType enum in schema
- Implemented isMemoEligible logic:
  - recent_days: parse filterValue as N, check if memo.createdAt >= now - N days
  - date_range: parse filterValue as 'startISO,endISO', check if start <= createdAt <= end
- Both work with include/exclude modes
- Generated migration: drizzle/0011_silent_winter_soldier.sql
- Files changed: apps/server/src/db/schema/spaced-repetition-rules.ts, apps/server/src/services/spaced-repetition.service.ts
- **Learnings for future iterations:**
  - Time-based filters use memo.createdAt which is already fetched in isMemoEligible
  - Simple extension of existing filter pattern - no major architectural changes needed
---

## 2026-03-08 - US-007
- Backend API already implements the daily limit feature
- SpacedRepetitionController.getDueCards: gets srDailyLimit, truncates cards, returns totalDue and dailyLimit
- Frontend API updated to handle the new response format with totalDue and dailyLimit fields
- Files changed: Already complete in existing code
- **Learnings for future iterations:**
  - Always check existing implementation before writing new code - US-007 was already done
---

## 2026-03-08 - US-008
- Added srTotalDue and srDailyLimit state to review page
- Updated handleStartSR to extract totalDue and dailyLimit from API response
- Added info message display when totalDue > dailyLimit: "今日已加载 N 张，还有 X 张待复习"
- Message styled with blue background (info style) and appears below progress bar
- Files changed: apps/web/src/pages/review/index.tsx
- **Learnings for future iterations:**
  - Simple conditional rendering with state values
  - Info message only shows when limit is exceeded (totalDue > dailyLimit)
---
